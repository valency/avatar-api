{
status	= {% if questions %} 200 {% else %} 500 {% endif %},
content	= {% if questions %} "argument 'questions' not provided" {% else %}
[{% for question in questions %}
{
id = "{{ question.id }}",
text = "{{ question.question_text }}"
},
{% endfor %}]
{% endif %}
